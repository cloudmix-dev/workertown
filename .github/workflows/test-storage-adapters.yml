name: Test storage adapters

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    branches:
      - main
    paths:
      - "/internal/storage/**"
      - .github/workflows/test-storage-adapters.yml
  workflow_dispatch:

jobs:
  test-storage-adapters:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Setup Node.js 18.x
        uses: actions/setup-node@v2
        with:
          node-version: 18.x

      - uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install Dependencies
        run: pnpm i

      - name: Start local services
        uses: isbang/compose-action@v1.5.1
        with:
          services: |
            upstash_redis
            dynamodb
            turso
            planetscale

      - name: Run Tests
        run: pnpm run test -- --filter=@workertown/{feature-flags,files,kv,search} -- 'test/storage/*.e2e.test.ts'
