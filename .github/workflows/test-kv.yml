name: Test @workertown/kv

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    branches:
      - main
    paths:
      - packages/kv/**
      - .github/workflows/test-kv.yml
  workflow_dispatch:

jobs:
  test-kv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Setup Node.js 18.x
        uses: actions/setup-node@v2
        with:
          node-version: 18.x

      - name: Install Dependencies
        run: npm -w @workertown/kv i

      - name: Start local services
        uses: isbang/compose-action@v1.5.1
        with:
          services: |
            upstash-redis
            dynamodb
            turso

      - name: Run Tests
        run: npm test -- --filter=@workertown/kv
